#container {
  box-sizing: content-box;
  width: 1196px;
  height: 100vh;
  padding: 48px 16px 32px;
  position: relative;
  overflow: hidden;
  margin: 0 auto;
  clear: both;
  display: flex;
  justify-content: space-between;
}

.page {
  width: 640px;
  padding-top: 24px;
}

.sidebar-right {
  width: 300px;
}

.sidebar-left {
  width: 192px;
}